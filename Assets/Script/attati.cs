using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class attati : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //FindAnyObjectByType
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
